{% extends 'base.html.twig' %}

{% block title %}
{# Append block form parent template with parent() function #}
Create a new Record | {{ parent() }}
{% endblock %}

{% block body %}
<h1>{{ title }}</h1>

{# TODO: add an image of the record #}

<div>
    Tracks: 

    <ul>
        {% for track in tracks %}
            <li>
                {{ track.song }} - {{ track.artist|upper }}
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
